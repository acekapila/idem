"""Optional local web GUI for idem. Requires `pip install idem-check[gui]`.

Launch with `idem gui`. The golden-set YAML remains the real source of
truth — this is a friendlier front end for editing, validating, and
running it, not a replacement for the file format.
"""
