"""Idem — deterministic drift detection for LLM APIs.

Detects when an LLM's response to a fixed set of test questions has
changed from an approved baseline, so you can catch a vendor silently
swapping the model behind a stable API name.
"""

__version__ = "0.1.0"
